import sys; sys.path.insert(0, 'src')
import pygame; pygame.init()
screen = pygame.display.set_mode((1280, 720))
import slime
s = slime.Slime((100, 100), 0)
print(f'Green slime HP: {s.hp} (survives {s.hp // 15} hits of 15 dmg)')
s = slime.Slime((100, 100), 1)
print(f'Blue slime HP: {s.hp}')
s = slime.Slime((100, 100), 2)
print(f'Red slime HP: {s.hp}')
s2 = slime.Slime((100, 100), 0)
s2.damage(15, source_velocity=(80, -60))
print(f'After 1 hit: hp={s2.hp}, dying={s2.dying}')
s2.damage(15, source_velocity=(80, -60))
s2.damage(15, source_velocity=(80, -60))
s2.damage(15, source_velocity=(80, -60))
print(f'After 4 hits: hp={s2.hp}, dying={s2.dying}, particles={len(s2.death_particles)}')
pygame.quit()
print('All OK')
